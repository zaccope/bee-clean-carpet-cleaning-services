# Images folder

Drop the real Bee Clean logo here as `LOGO.jpg` (or `LOGO.png` — update references in the code if so) to replace the placeholder.

Required:
- `LOGO.jpg` — square logo, used in nav, hero card, favicon, and Open Graph image.

Recommended: 400×400 minimum, square crop, JPG or PNG.
